# Runtime dependencies (aligned with pyproject.toml [project.dependencies]).
# CI and recommended dev setup: pip install -e ".[dev]"
# eval-type-backport: required on Python 3.9 so Pydantic can evaluate PEP 604 unions (int | None, etc.).
pyspark>=4.0.0
pydantic>=2.5.0
python-dateutil>=2.8.0
tiktoken>=0.7.0
eval-type-backport>=0.3.1; python_version < "3.10"
